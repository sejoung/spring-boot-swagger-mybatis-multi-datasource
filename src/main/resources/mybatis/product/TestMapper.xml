<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enliple.recommend.api.product.dao.TestDao">
    <select id="selectTest" resultType="Test">		
    select 1 as id from dual
	</select>
	 <select id="selectAdmember" resultType="java.util.HashMap">		
    	select * from admember limit 100
	</select>
</mapper>